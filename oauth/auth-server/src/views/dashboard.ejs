<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Notes Service</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background: #f5f7fa;
      min-height: 100vh;
    }
    .navbar {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 16px 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .navbar-brand {
      display: flex;
      align-items: center;
      gap: 12px;
      font-size: 20px;
      font-weight: 600;
    }
    .navbar-user {
      display: flex;
      align-items: center;
      gap: 16px;
    }
    .avatar {
      width: 36px;
      height: 36px;
      background: rgba(255,255,255,0.2);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
    }
    .btn {
      padding: 8px 16px;
      border-radius: 6px;
      text-decoration: none;
      font-size: 14px;
      font-weight: 500;
    }
    .btn-light {
      background: rgba(255,255,255,0.2);
      color: white;
    }
    .btn-light:hover {
      background: rgba(255,255,255,0.3);
    }
    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 24px;
    }
    .card {
      background: white;
      border-radius: 12px;
      padding: 24px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      margin-bottom: 24px;
    }
    .card h2 {
      font-size: 20px;
      color: #333;
      margin-bottom: 16px;
    }
    .user-info {
      display: grid;
      gap: 12px;
    }
    .info-row {
      display: flex;
      padding: 12px;
      background: #f8f9fa;
      border-radius: 8px;
    }
    .info-label {
      width: 120px;
      color: #666;
      font-weight: 500;
    }
    .info-value {
      color: #333;
    }
    .actions {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
    }
    .action-card {
      padding: 24px;
      background: #f8f9fa;
      border-radius: 12px;
      text-align: center;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .action-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 8px 24px rgba(0,0,0,0.1);
    }
    .action-icon {
      font-size: 40px;
      margin-bottom: 12px;
    }
    .action-title {
      font-weight: 600;
      color: #333;
      margin-bottom: 8px;
    }
    .action-desc {
      font-size: 14px;
      color: #666;
      margin-bottom: 16px;
    }
    .action-btn {
      display: inline-block;
      padding: 10px 20px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      font-size: 14px;
    }
    .action-btn:hover {
      opacity: 0.9;
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-brand">
      <span>🔐</span>
      <span>Notes Service</span>
    </div>
    <div class="navbar-user">
      <div class="avatar"><%= user.username.charAt(0).toUpperCase() %></div>
      <span><%= user.username %></span>
      <a href="/logout" class="btn btn-light">Logout</a>
    </div>
  </nav>
  
  <div class="container">
    <h1 style="margin-bottom: 24px; color: #333;">👋 Welcome, <%= user.username %>!</h1>
    
    <div class="card">
      <h2>👤 Your Profile</h2>
      <div class="user-info">
        <div class="info-row">
          <div class="info-label">Username</div>
          <div class="info-value"><%= user.username %></div>
        </div>
        <div class="info-row">
          <div class="info-label">Email</div>
          <div class="info-value"><%= user.email %></div>
        </div>
        <div class="info-row">
          <div class="info-label">User ID</div>
          <div class="info-value" style="font-family: monospace; font-size: 13px;"><%= user.id %></div>
        </div>
        <div class="info-row">
          <div class="info-label">Member Since</div>
          <div class="info-value"><%= new Date(user.createdAt).toLocaleDateString() %></div>
        </div>
      </div>
    </div>
    
    <div class="card">
      <h2>🚀 Quick Actions</h2>
      <div class="actions">
        <div class="action-card">
          <div class="action-icon">📝</div>
          <div class="action-title">My Notes</div>
          <div class="action-desc">View and manage your notes</div>
          <a href="http://localhost:4001" class="action-btn" target="_blank">Open Notes App</a>
        </div>
        <div class="action-card">
          <div class="action-icon">📊</div>
          <div class="action-title">Analytics</div>
          <div class="action-desc">See insights from your notes</div>
          <a href="http://localhost:4002" class="action-btn" target="_blank">Open Analytics</a>
        </div>
      </div>
    </div>
    
    <div class="card" style="background: #fef3c7; border: 1px solid #fbbf24;">
      <h2 style="color: #92400e;">🔑 OAuth Info</h2>
      <p style="color: #92400e; font-size: 14px; line-height: 1.6;">
        This is the <strong>Authorization Server</strong>. Third-party apps can request access to your notes through OAuth 2.0.
        When an app requests access, you'll be asked to grant permission here.
      </p>
    </div>
  </div>
</body>
</html>
